name: Label Issue
on: 
  issues:
    types:
  
jobs:
  label-issues:
    runs-on: ubuntu-latest
     permissions:
        issues: write
  steps:
  - name: Label issues
    uses: andymckay/labeler@1.0.2
    with:
      add-labels: "triage"
      repo-token: $({secrete_GITHUB_TOKEN})
